#pragma once
#include <string>

// Imprime todas las permutaciones de la cadena s.
// Caso base: se fija la última posición y se imprime.
// Precondición: ninguna; para s vacía imprime "(vacía)".
void permutaciones(const std::string& s);

